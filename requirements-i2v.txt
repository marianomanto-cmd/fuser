# ============================================================================
# Fuser — dependencias OPCIONALES de la función "Imagen → Vídeo" (Wan 2.2 I2V).
#
# La función habla con un ComfyUI local SOLO con la librería estándar (urllib),
# así que la app arranca sin nada de esto. Esta única dependencia es opcional y
# muy ligera: mejora la BARRA DE PROGRESO en vivo (paso x/y) durante la
# generación. Sin ella, se usa 'polling' (progreso más grueso, pero funciona).
#
#   pip install -r requirements-i2v.txt
#
# El motor pesado (Wan 2.2 14B, GGUF, offloading) y el audio (Stable Audio Open)
# viven en una instalación de ComfyUI APARTE: ver docs/IMAGE_TO_VIDEO.md.
# ============================================================================

websocket-client>=1.7.0
